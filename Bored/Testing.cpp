#include "ArrayList.h"
#include <iostream>
#include <array>

using namespace std;

int main(){

    array<int,10> arr {0};

    // ArrayList<int> array;

    // array.addFirst(10);
    // array.addFirst(15);
    // array.addLast(20);

    // auto iterator = array.begin();

    // for (int i = 0 ; i < 4; i++) {
    //     cout << iterator.get() << endl;
    //     iterator.next();
    // }

    // for (int i = 0; i < 10; i++)
    // { 
    //     cout << i;
    // }

    return 0;
}